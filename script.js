document.querySelectorAll('.input-container input').forEach(input => {
            input.addEventListener('input', function() {
                // Метка автоматически скрывается через CSS когда поле не пустое
            });
        });